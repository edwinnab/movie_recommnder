# movie_recommnder
simple movie recommender system using python pandas library.
recommender system employs statistical algorithm that seeks to prdict user's ratings for a particular entity, based on the similarity between the entities or similarity between the users that previously rated those entities.

dataset used is "MovieLens Dataset" it uses correlation between ratings assigned to different movies in ordr to find the similarity between the movies.
